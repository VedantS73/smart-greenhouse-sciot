# Python dependencies for the Smart Greenhouse services
#
# Install with:  pip install -r requirements.txt
#
# Note: grovepi (Grove Pi hardware I/O) and pyperplan (PDDL planner) are also
# required on the Raspberry Pi but are installed separately / platform-specific:
#   pip install pyperplan
#   grovepi comes from the Dexter Industries GrovePi setup.

paho-mqtt>=1.6,<2.0
python-dotenv>=1.0
twilio>=8.0
requests>=2.28
